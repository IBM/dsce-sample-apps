# Direct dependencies only. Lower bounds are the versions this app was verified with;
# leaving upper room lets a rebuild pick up patched releases instead of pinning known CVEs.
# (The previous file was a full `pip freeze` of a dev machine, including Jupyter/notebook
# packages the app never imports.)
dash>=3.3.0,<4
dash-bootstrap-components>=2.0.4,<3
pandas>=2.2.3,<3
python-dotenv>=1.2.1,<2
jproperties>=2.1.2,<3
# metrics + llmaj extras provide MetricsEvaluator, the built-in guardrail metrics and LLMJudge
ibm-watsonx-gov[metrics,llmaj]>=1.4.0,<2
